#version 460 core

layout (local_size_x = 32, local_size_y = 32) in;

//NOTE: not compiled as part of this sample, but left as a reference
//of what a skeleton compute shader looks like

void main(void)
{
    //Do nothing
}
